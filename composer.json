{
  "name": "codicastudio/static-header",
  "description": "A noobs worthless garbage, just learning.",
  "keywords": [
      "worthless garabage by a noob."
  ],
  "license": "MIT",
  "require": {
    "php": ">=7.4.8",
    "codicastudio/responsive": "^2.0.0"
  },
  "autoload": {
    "psr-4": {
      "StaticHeader\\": "src/"
    }
  },
  "extra": {
    "laravel": {
      "providers": [
        "StaticHeader\\StaticHeaderServiceProvider"
      ]
    }
  },
  "scripts": {
    "test": "vendor/bin/phpunit",
    "test-coverage": "vendor/bin/phpunit --coverage-html coverage"
},
  "config": {
    "sort-packages": true
  },
  "minimum-stability": "dev",
  "prefer-stable": true
}
